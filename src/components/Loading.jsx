import React, { Component } from 'react';

export class Loading extends Component {
  render() {
    return (
      <div>Carregando...</div>

    );
  }
}

Loading.propTypes = {
};

export default Loading;
